const VisaSubmitForm = {
    fromCountry: '#from_country',
    toCountry: '#to_country',
    date: '[name="checkin"]',
    submit: '#submit'
}

const VisaSubmissionForm = {
    firstname: '[name="first_name"]',
    email: '[name="email"]',
    date: '[name="date"]',
    submit: '#submit'
}

const SubmittedVisa = {
    header: '.sec__title_list',
    info: '.card-body >h2'
}

export { VisaSubmitForm, VisaSubmissionForm, SubmittedVisa }